apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: 5000       # 서비스 포트
      targetPort: 5000 # 컨테이너 포트
      nodePort: 30007  # 외부에서 접속할 수 있는 포트 (NodePort)
  type: NodePort
